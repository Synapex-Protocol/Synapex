{
  "name": "synx-bnb-deployment",
  "version": "1.0.0",
  "description": "SYNAPEX Protocol deployment package for BNB Chain",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.js --network bnb-testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network bnb-mainnet"
  },
  "devDependencies": {
    "hardhat": "^2.19.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "ethers": "^6.9.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
